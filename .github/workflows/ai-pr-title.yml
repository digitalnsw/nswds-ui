# Synced from digitalnsw/nswds-devops — DO NOT EDIT in this repo.
# The implementation lives in that repo's reusable-ai-pr-title.yml.
name: AI PR Title Generator

on:
  pull_request:
    types: [opened, reopened, synchronize, edited]

permissions:
  contents: read
  pull-requests: write

jobs:
  generate-title:
    uses: digitalnsw/nswds-devops/.github/workflows/reusable-ai-pr-title.yml@v1
    secrets:
      # Explicit mapping, not inherit — this workflow needs exactly one secret,
      # and inherit would hand every repo secret to the called workflow.
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
