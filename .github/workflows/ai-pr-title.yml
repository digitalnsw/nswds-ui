name: AI PR Title Generator

'on':
  pull_request:
    types: [opened, reopened, synchronize, edited]

jobs:
  generate-title:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    env:
      OPENAI_MODEL: gpt-4o-mini
      CONVENTIONAL_COMMIT_REGEX_DEFAULT: '^(feat|fix|refactor|perf|style|test|build|ops|docs|chore|merge|revert)(\([^)]*\))?!?: .+'
      CONVENTIONAL_COMMIT_TYPES_CSV_DEFAULT: 'feat, fix, refactor, perf, style, test, build, ops, docs, chore, merge, revert'
      CONVENTIONAL_COMMIT_DEFAULT_TYPE_DEFAULT: chore

    steps:
      - name: Checkout code
        if: ${{ github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Load Conventional Commit config
        id: conventional-config
        if: ${{ github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        shell: bash
        run: |
          set -euo pipefail
          REGEX="$(./scripts/conventional-commit-config.sh regex)"
          TYPES_CSV="$(./scripts/conventional-commit-config.sh csv)"
          DEFAULT_TYPE="$(./scripts/conventional-commit-config.sh safe-default)"
          {
            printf 'regex=%s\n' "$REGEX"
            printf 'types_csv=%s\n' "$TYPES_CSV"
            printf 'default_type=%s\n' "$DEFAULT_TYPE"
          } >> "$GITHUB_OUTPUT"

      - name: Detect commitlint config
        id: commitlint-config
        if: ${{ github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        shell: bash
        run: |
          set -euo pipefail
          present=false
          for f in commitlint.config.js commitlint.config.cjs commitlint.config.mjs \
                   .commitlintrc .commitlintrc.js .commitlintrc.cjs .commitlintrc.json \
                   .commitlintrc.yml .commitlintrc.yaml; do
            if [[ -f "$f" ]]; then present=true; echo "Found commitlint config: $f"; break; fi
          done
          echo "present=$present" >> "$GITHUB_OUTPUT"

      - name: Setup Node.js
        # commitlint v21 requires Node >=22.12.0 — pin instead of relying on lts/*.
        if: ${{ steps.commitlint-config.outputs.present == 'true' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install commitlint
        if: ${{ steps.commitlint-config.outputs.present == 'true' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        shell: bash
        run: npm install --no-save --no-fund --no-audit --loglevel=error @commitlint/cli @commitlint/config-conventional

      - name: Get Current PR Title
        id: get-pr-title
        uses: actions/github-script@v8
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            console.log(`Current PR title: "${pr.title}"`);
            core.setOutput("pr_title", pr.title || "");

            // Recover the title this workflow last generated, if any. It's stored
            // as a base64 marker in the PR body, letting us tell an auto-generated
            // title (safe to refresh when new commits land) from a human-authored
            // one (which we must never clobber).
            const body = pr.body || "";
            const m = body.match(/<!--\s*ai-pr-title-marker:\s*([A-Za-z0-9+/=]+)\s*-->/);
            let markerTitle = "";
            if (m) {
              try { markerTitle = Buffer.from(m[1], "base64").toString("utf8").trim(); }
              catch (e) { markerTitle = ""; }
            }
            console.log(`Recovered auto-title marker: "${markerTitle}"`);
            core.setOutput("ai_marker_title", markerTitle);

      - name: Validate PR Title
        id: validate-title
        shell: bash
        env:
          PR_TITLE: ${{ steps.get-pr-title.outputs.pr_title }}
          ORIGINAL_TITLE: ${{ github.event.pull_request.title }}
          CONVENTIONAL_COMMIT_REGEX: ${{ steps.conventional-config.outputs.regex || env.CONVENTIONAL_COMMIT_REGEX_DEFAULT }}
          USE_COMMITLINT: ${{ steps.commitlint-config.outputs.present }}
          AI_MARKER_TITLE: ${{ steps.get-pr-title.outputs.ai_marker_title }}
          EVENT_ACTION: ${{ github.event.action }}
        run: |
          set -euo pipefail

          PR_TITLE="${PR_TITLE-}"
          REGEX="${CONVENTIONAL_COMMIT_REGEX}"

          echo "🔍 Current PR Title: $PR_TITLE"

          # Validate with commitlint when it's installed (full ruleset: type-enum,
          # subject-case, full-stop, header length…), so titles that pass the
          # regex but violate a commitlint rule are flagged for regeneration —
          # not just failed at the end. Fall back to the regex otherwise.
          title_is_valid() {
            [[ -n "$PR_TITLE" ]] || return 1
            if [[ "${USE_COMMITLINT:-}" == "true" ]]; then
              printf '%s' "$PR_TITLE" | npx --no-install commitlint >/dev/null 2>&1
              return
            fi
            [[ "$PR_TITLE" =~ $REGEX ]]
          }

          if title_is_valid; then
            # A valid title is normally left alone. The one exception: new commits
            # arrived (synchronize) and the current title is one *we* generated
            # (matches the marker we stored in the PR body). That title may now be
            # stale, so refresh it against the latest commits. A human-authored
            # title won't match the marker and is never touched.
            if [[ "${EVENT_ACTION:-}" == "synchronize" && -n "${AI_MARKER_TITLE:-}" && "$PR_TITLE" == "$AI_MARKER_TITLE" ]]; then
              echo "♻️ PR Title is valid but auto-generated; refreshing against the latest commits."
              echo "update_needed=true" >> "$GITHUB_OUTPUT"
              echo "refresh_auto=true" >> "$GITHUB_OUTPUT"
            else
              echo "✅ PR Title is already valid."
              echo "update_needed=false" >> "$GITHUB_OUTPUT"
              echo "refresh_auto=false" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "❌ PR Title is missing or not in Conventional Commits format."
            echo "update_needed=true" >> "$GITHUB_OUTPUT"
            echo "refresh_auto=false" >> "$GITHUB_OUTPUT"
          fi

          # Preserve the title from the triggering event so we can avoid overwriting a human edit mid-run.
          # (Using the event payload avoids race windows between earlier steps.)
          ORIGINAL_TITLE="${ORIGINAL_TITLE-}"
          DELIM="TITLE_$(date +%s)_$RANDOM"
          {
            echo "original_title<<$DELIM"
            printf '%s\n' "$ORIGINAL_TITLE"
            echo "$DELIM"
          } >> "$GITHUB_OUTPUT"

      - name: 'Note: Skipped (PR from fork)'
        if: ${{ github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name }}
        shell: bash
        run: |
          echo "ℹ️ Skipping: PR is from a fork, so repo secrets / write permissions may be unavailable."

      - name: Get commit messages
        id: commits
        if: ${{ steps.validate-title.outputs.update_needed == 'true' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        uses: actions/github-script@v8
        env:
          CONVENTIONAL_COMMIT_REGEX: ${{ steps.conventional-config.outputs.regex || env.CONVENTIONAL_COMMIT_REGEX_DEFAULT }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const pull_number = context.payload.pull_request.number;

            const commits = await github.paginate(github.rest.pulls.listCommits, {
              owner,
              repo,
              pull_number,
              per_page: 100,
            });

            const subjects = commits
              .map(c => (c.commit?.message || '').split('\n')[0].trim())
              .filter(Boolean);

            const conventional = new RegExp(process.env.CONVENTIONAL_COMMIT_REGEX);
            const selected = subjects.filter(s => conventional.test(s));
            const out = (selected.length ? selected : subjects).join('\n');

            if (!out) {
              core.setOutput('commits', '');
              core.setOutput('commits_b64', '');
              console.log('❌ No commit subjects available for this PR.');
              return;
            }

            // Do not log commit subjects.
            console.log(`📝 Commit subjects found: ${subjects.length} (conventional: ${selected.length})`);
            core.setOutput('commits', out);
            core.setOutput('commits_b64', Buffer.from(out, 'utf8').toString('base64'));

      - name: Ensure jq is available
        if: ${{ steps.validate-title.outputs.update_needed == 'true' && steps.commits.outputs.commits != '' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v jq >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y jq
          fi

      - name: Generate PR title with OpenAI (Responses API)
        id: ai
        if: ${{ steps.validate-title.outputs.update_needed == 'true' && steps.commits.outputs.commits != '' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        shell: bash
        env:
          COMMITS_B64: ${{ steps.commits.outputs.commits_b64 }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          CONVENTIONAL_COMMIT_REGEX: ${{ steps.conventional-config.outputs.regex || env.CONVENTIONAL_COMMIT_REGEX_DEFAULT }}
          CONVENTIONAL_COMMIT_TYPES_CSV: ${{ steps.conventional-config.outputs.types_csv || env.CONVENTIONAL_COMMIT_TYPES_CSV_DEFAULT }}
          DEFAULT_CONVENTIONAL_COMMIT_TYPE: ${{ steps.conventional-config.outputs.default_type || env.CONVENTIONAL_COMMIT_DEFAULT_TYPE_DEFAULT }}
        run: |
          set -euo pipefail

          if [[ -z "${COMMITS_B64-}" ]]; then
            echo "❌ Missing commit list (base64)."
            exit 1
          fi

          COMMIT_LIST="$(printf '%s' "$COMMITS_B64" | base64 -d)"

          # Don't echo commit messages. Keep a small breadcrumb.
          COMMIT_COUNT="$(printf '%s\n' "$COMMIT_LIST" | sed '/^[[:space:]]*$/d' | wc -l | tr -d ' ')"
          echo "📝 Commit subjects provided to the model: $COMMIT_COUNT"

          JSON_PAYLOAD="$(jq -n --arg commits "$COMMIT_LIST" --arg model "$OPENAI_MODEL" --arg allowedTypes "$CONVENTIONAL_COMMIT_TYPES_CSV" '{
            model: $model,
            input: [
              {
                role: "system",
                content: [
                  {
                    type: "input_text",
                    text: ("You generate PR titles following Conventional Commits: type(scope): description (or type: description). Allowed types: " + $allowedTypes + ". Output ONLY the title line (no quotes, no code fences, no prefixes like Title:).")
                  }
                ]
              },
              {
                role: "user",
                content: [
                  {
                    type: "input_text",
                    text: ("Commit subjects:\n\n" + $commits + "\n\nGenerate exactly one PR title in Conventional Commits format.")
                  }
                ]
              }
            ],
            temperature: 0.4,
            max_output_tokens: 80
          }')"

          API_KEY="${OPENAI_API_KEY-}"
          if [[ -z "${API_KEY:-}" ]]; then
            echo "❌ Missing secrets.OPENAI_API_KEY. Add it in repo settings."
            exit 1
          fi

          set +e
          RESPONSE="$(
            curl -sS --fail-with-body "https://api.openai.com/v1/responses" \
              -H "Authorization: Bearer ${API_KEY}" \
              -H "Content-Type: application/json" \
              -d "$JSON_PAYLOAD"
          )"
          curl_status=$?
          set -e

          if [[ $curl_status -ne 0 ]]; then
            echo "❌ OpenAI API request failed (curl exit code: $curl_status)."
            echo "$RESPONSE" | head -c 400
            echo
            exit 1
          fi

          # Fail fast with a clear message if the API returns non-JSON (auth/proxy errors can be HTML).
          if ! echo "$RESPONSE" | jq -e . >/dev/null 2>&1; then
            echo "❌ OpenAI API returned a non-JSON response."
            echo "$RESPONSE" | head -c 400
            echo
            exit 1
          fi

          if echo "$RESPONSE" | jq -e '.error' >/dev/null; then
            errType="$(echo "$RESPONSE" | jq -r '.error.type // "unknown"')"
            errMsg="$(echo "$RESPONSE" | jq -r '.error.message // ""' | head -c 200)"
            echo "❌ OpenAI API error ($errType): $errMsg"
            exit 1
          fi

          RAW_TITLE="$(echo "$RESPONSE" | jq -r '
            [(.output[]? | select(.type=="message") | .content[]? | select(.type=="output_text") | .text)] | join("")
          ' | sed -e 's/\r//g' | sed -n '/^[[:space:]]*[^[:space:]]/ {p; q;}')"

          if [[ -z "${RAW_TITLE:-}" ]]; then
            echo "❌ Could not extract title from response (no output_text)."
            exit 1
          fi

          TITLE="$(printf '%s' "$RAW_TITLE" | head -n 1 | tr '\n' ' ' | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')"
          TITLE="$(printf '%s' "$TITLE" | sed -E 's/^Title:[[:space:]]*//I')"
          TITLE="$(printf '%s' "$TITLE" | sed -E 's/^["'\''`]+|["'\''`]+$//g')"
          TITLE="$(printf '%s' "$TITLE" | sed -E 's/^```[a-zA-Z0-9_-]*//; s/```$//')"
          TITLE="$(printf '%s' "$TITLE" | sed -E 's/[[:space:]]+/ /g')"

          REGEX="${CONVENTIONAL_COMMIT_REGEX}"
          if [[ ! "$TITLE" =~ $REGEX ]]; then
            echo "⚠️ AI title failed validation."

            FIRST_LINE="$(printf '%s\n' "$COMMIT_LIST" | sed -n '/[^[:space:]]/ {p; q;}')"
            if [[ -n "$FIRST_LINE" && "$FIRST_LINE" =~ $REGEX ]]; then
              TITLE="$FIRST_LINE"
              echo "↪️ Falling back to first conventional subject."
            else
              TITLE="${DEFAULT_CONVENTIONAL_COMMIT_TYPE}: update"
              echo "↪️ Falling back to generic title."
            fi
          else
            echo "🧠 AI suggested title: $TITLE"
          fi

          DELIM="TITLE_$(date +%s)_$RANDOM"
          {
            echo "title<<$DELIM"
            printf '%s\n' "$TITLE"
            echo "$DELIM"
          } >> "$GITHUB_OUTPUT"

      - name: Update PR Title if Invalid or Empty
        # Gate on having a suggested title; prevents hard-fail when AI didn't run.
        if: ${{ steps.validate-title.outputs.update_needed == 'true' && steps.ai.outputs.title != '' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        uses: actions/github-script@v8
        env:
          CONVENTIONAL_COMMIT_REGEX: ${{ steps.conventional-config.outputs.regex || env.CONVENTIONAL_COMMIT_REGEX_DEFAULT }}
          REFRESH_AUTO: ${{ steps.validate-title.outputs.refresh_auto }}
        with:
          script: |
            const suggestedTitle = ${{ toJson(steps.ai.outputs.title) }};
            const originalTitle = ${{ toJson(steps.validate-title.outputs.original_title) }};
            const refreshAuto = process.env.REFRESH_AUTO === 'true';

            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });

            const conventional = new RegExp(process.env.CONVENTIONAL_COMMIT_REGEX);

            if (pr.title !== originalTitle) {
              console.log(`ℹ️ PR title changed during the run. Skipping update. Current: "${pr.title}"`);
              return;
            }

            // Don't clobber a valid, human-authored title. The refresh path is the
            // sole exception: the title is valid *and* matches our stored marker,
            // so it's our own auto-title and safe to regenerate.
            if (conventional.test(pr.title || "") && !refreshAuto) {
              console.log(`ℹ️ PR title is already valid (human-authored). Skipping update. Current: "${pr.title}"`);
              return;
            }

            // Build a single update: the new title (if changed) plus a refreshed
            // base64 marker in the body so future runs recognise this auto-title.
            const update = {};
            if (suggestedTitle !== pr.title) update.title = suggestedTitle;

            const marker = `<!-- ai-pr-title-marker: ${Buffer.from(suggestedTitle, "utf8").toString("base64")} -->`;
            const body = pr.body || "";
            const stripped = body.replace(/\n*<!--\s*ai-pr-title-marker:[\s\S]*?-->/g, "").trimEnd();
            const newBody = stripped ? `${stripped}\n\n${marker}` : marker;
            if (newBody !== body) update.body = newBody;

            if (Object.keys(update).length === 0) {
              console.log(`ℹ️ Title and marker already current. Nothing to update.`);
              return;
            }

            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              ...update
            });

            console.log(`✅ PR updated. Title: "${update.title ?? pr.title}"${update.body ? " (marker refreshed)" : ""}`);

      - name: 'Note: Skipped update (no title available)'
        if: ${{ steps.validate-title.outputs.update_needed == 'true' && steps.ai.outputs.title == '' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name }}
        shell: bash
        run: |
          echo "ℹ️ Update was needed, but no suggested title was available (commit extraction or AI step may have been skipped)."

      # Fetch the title as it stands now (after any update above) so the lint
      # below reflects what will actually become the squash commit subject.
      - name: Get final PR title
        id: final-title
        uses: actions/github-script@v8
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            console.log(`Final PR title: "${pr.title}"`);
            core.setOutput("title", pr.title || "");

      # Reuses Node + commitlint installed earlier; gated on the same detection.
      # Skips cleanly in repos that haven't adopted commitlint yet.
      - name: Lint final PR title with commitlint
        if: ${{ steps.commitlint-config.outputs.present == 'true' && steps.final-title.outputs.title != '' }}
        shell: bash
        env:
          FINAL_TITLE: ${{ steps.final-title.outputs.title }}
        run: |
          set -euo pipefail
          echo "🔍 Linting final PR title against commitlint: $FINAL_TITLE"
          printf '%s' "$FINAL_TITLE" | npx --no-install commitlint
