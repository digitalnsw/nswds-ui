# Synced from digitalnsw/nswds-devops — DO NOT EDIT in this repo.
# The implementation lives in that repo's reusable-validate-branch-name.yml.
name: Validate Branch Name

on:
  pull_request:
    types: [opened, edited, reopened]

permissions:
  contents: read

jobs:
  check-branch-name:
    uses: digitalnsw/nswds-devops/.github/workflows/reusable-validate-branch-name.yml@main
    secrets: inherit
